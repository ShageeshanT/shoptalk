# Approval Bottleneck

Adds `classify_approval_bottleneck` so the seller dashboard can show a compact label for approval bottleneck.

Labels:

- Clear approvals
- Approval queue
- Approval bottleneck
